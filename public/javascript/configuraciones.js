
options = {
    method: "GET",
    credentials: "same-origin",
    headers: {
      "Content-Type": "application/json"
    }
};

start = () =>{
    var btn_pass = document.getElementById("editp");
    var btn_email = document.getElementById("edite");

    //btn pass
    btn_pass.addEventListener("click", e => {
        
    });
}

window.onload = start;