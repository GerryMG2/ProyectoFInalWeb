nav.navbar(role='navigation' aria-label='main navigation')
  .navbar-brand
    a#topNav.navbar-burger.burger(role='button' aria-label='menu' aria-expanded='false' data-target='navbarBasicExample')
      span(aria-hidden='true')
      span(aria-hidden='true')
      span(aria-hidden='true')
  #navbarBasicExample.navbar-menu
      a.navbar-item.is-active.navbar-end.is-four-fifths(href='/main')
        | Laboratorios UCA
      if !usad 
        .navbar-item.has-dropdown.is-hoverable
          a.navbar-link
            | Cuenta
          .navbar-dropdown
            a.navbar-item(href='/main')
              | Principal 
            a.navbar-item(href='/reservas')
              | Hacer reserva
            hr.navbar-divider
            a.navbar-item(href="/profile")
              |Perfil
            a.navbar-item(href='/logout')
              | Logout
      else
        .navbar-item.has-dropdown.is-hoverable
          a.navbar-link
            | Administrar
          .navbar-dropdown
            a.navbar-item(href='/main')
              | Main 
            a.navbar-item(href='/admin/laboratorios')
              | Laboratorios
            a.navbar-item(href='/admin/users')
              | Usuarios
            a.navbar-item(href='/admin/reservas')
              | Reservas
            hr.navbar-divider
            a.navbar-item(href='/reservas')
              | Hacer reservas
            a.navbar-item(href="/profile")
              | Profile
            a.navbar-item(href='/logout')
              | Logout

